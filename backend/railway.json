{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "startCommand": "sh -c 'node prisma/bootstrap-migrations.js --adopt-existing && node prisma/seed-prod.js && node dist/main'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
